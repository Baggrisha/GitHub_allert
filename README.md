
# GitHubBot

Телеграм-бот для отслеживания коммитов, управления списком репозиториев и админ-панели.

## Функциональность

* 📌 Отслеживание последних коммитов в GitHub репозиториях
* ➕ Добавление и удаление репозиториев для отслеживания
* 📨 Отправка уведомлений о новых коммитах администраторам
* 🗄️ SQLite база данных для хранения списка репозиториев
* 🔄 Асинхронная работа с GitHub API и Telegram через `aiogram` и `aiohttp`

## Быстрый старт

1. Клонируйте репозиторий:

```bash
git clone https://github.com/Baggrisha/githubbot.git
cd githubbot
```

2. Скопируйте `.env.example` в `.env` и заполните токены:

```bash
cp .env.example .env
```

3. Отредактируйте `.env` файл, указав ваши токены и настройки:

* `BOT_TOKEN` — токен Telegram бота
* `GITHUB_ACCESS_TOKEN` — токен GitHub
* `DB_PATH` — путь к SQLite базе данных
* `ADMIN_USER_ID` — ID админов, которые будут получать уведомления

4. Запуск через Docker:

```bash
docker compose up -d --build
```

---

## Структура проекта

```
githubbot/
├── app/
│   ├── main.py                     # Запуск бота
│   ├── db.py                       # Работа с базой данных
│   ├── main_handler.py             # Обработчик
│   ├── config.py                   # Настройки и токены
│   ├── bot.py                      # Инициализация бота
│   └── middlewares.py              # Middlewares
├── data/                           # SQLite база данных
├── docker-compose.yml              # Docker Compose конфигурация
├── Dockerfile                      # Docker образ
└── requirements.txt                # Python зависимости
```

---

## Команды бота

* `/last_commit <repo>` — последние коммиты указанного репозитория
* `/last_commits` — последние коммиты всех репозиториев из базы
* `/add_repo <repo>` — добавить репозиторий в базу для отслеживания
* `/remove_repo <repo>` — удалить репозиторий из базы

💡 `<repo>` может быть в формате `owner/repo` или URL `https://github.com/owner/repo`.

---

## Локальная установка зависимостей

```bash
pip install -r requirements.txt
```

Для локального тестирования:

```bash
python -m app.main
```

---

## Пример уведомления о коммите

```
📌 Последние коммиты в репо: Baggrisha/GitHub_allert

💬 ✅ Обновление загружено:
Исправлен баг с авторизацией

📝 Комментарий:
Добавлены проверки токена

👤 Автор: Baggrisha
🕒 Дата коммита: 20:15:30 20.11.2025
🧬 SHA: 1a2b3c4
🔗 Ссылка: открыть на GitHub
```

